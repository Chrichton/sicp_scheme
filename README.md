Evaluate Code

- First time: Terminal -> Ausgabe ausführen -> Run Scheme
- Focus Terminal + Ctrl-Shift-R

To **focus** on the open **editor** in Visual Studio Code, you can use the following keyboard shortcut:
**Ctrl + 1**

To select or **focus** on the currently open **terminal** in Visual Studio Code, you can use the following keyboard shortcut:
**Ctrl + `** (the backtick key)
